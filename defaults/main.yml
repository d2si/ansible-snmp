---

snmpd:
    cfg:
      path: "{{ osvar['cfg']['path'] }}"
      agentaddress: "{{ snmpd['cfg']['agentaddress'] }}"
      agentuser: root
      rocommunity:
        from: "{{ ansible_default_ipv4.network }}/{{ ansible_default_ipv4.netmask }}"
        name: oxalide
      syslocation: oxalide
      syscontact: report@oxalide.com
    pkg:
      name: "{{ osvar['pkg']['name'] }}"
    svc:
      name: "{{ osvar['svc']['name'] }}"
      enabled: yes
