---

snmpd:
    cfg:
      path: "{{ osvar['cfg']['path'] }}"
      syslocation: Sitting on the Dock of the Bay
      sysContact: Me <me@example.org>
    pkg:
      name: "{{ osvar['pkg']['name'] }}"
    svc:
      name: "{{ osvar['svc']['name'] }}"
      enabled: yes
